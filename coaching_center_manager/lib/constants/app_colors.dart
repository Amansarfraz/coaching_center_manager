import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ========== Primary Colors ==========
  static const Color primary = Color(0xFF2563EB);
  static const Color primaryLight = Color(0xFF60A5FA);
  static const Color primaryDark = Color(0xFF1D4ED8);

  // ========== Secondary ==========
  static const Color secondary = Color(0xFF0EA5E9);

  // ========== Background ==========
  static const Color scaffoldBackground = Color(0xFFF8FAFC);
  static const Color card = Colors.white;

  // ========== Text ==========
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color hint = Color(0xFF94A3B8);

  // ========== Border ==========
  static const Color border = Color(0xFFE2E8F0);

  // ========== Status Colors ==========
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // ========== Fee Status ==========
  static const Color paid = Color(0xFF22C55E);
  static const Color pending = Color(0xFFEF4444);
  static const Color partial = Color(0xFFF59E0B);

  // ========== Attendance ==========
  static const Color present = Color(0xFF16A34A);
  static const Color absent = Color(0xFFDC2626);
  static const Color leave = Color(0xFFFACC15);

  // ========== Dashboard Cards ==========
  static const Color studentCard = Color(0xFF2563EB);
  static const Color teacherCard = Color(0xFF8B5CF6);
  static const Color batchCard = Color(0xFF0EA5E9);
  static const Color feeCard = Color(0xFF10B981);
  static const Color attendanceCard = Color(0xFFF97316);

  // ========== Others ==========
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color transparent = Colors.transparent;
}
