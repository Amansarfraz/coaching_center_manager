import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Colors
  static const Color primary = Color(0xFF2F80ED);
  static const Color primaryDark = Color(0xFF1B5FC1);
  static const Color secondary = Color(0xFFF2994A);

  // Backgrounds
  static const Color scaffoldBackground = Color(0xFFF5F8FF);
  static const Color surface = Colors.white;
  static const Color cardBackground = Colors.white;

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF7A7A7A);
  static const Color textOnPrimary = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF27AE60);
  static const Color error = Color(0xFFEB5757);
  static const Color warning = Color(0xFFF2C94C);
  static const Color info = Color(0xFF2F80ED);

  // Borders / Dividers
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEFEFEF);

  // Attendance Status
  static const Color present = Color(0xFF27AE60);
  static const Color absent = Color(0xFFEB5757);
  static const Color leave = Color(0xFFF2C94C);
}
